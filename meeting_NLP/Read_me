一个最基础的唐诗生成器，原版本时Tensflow 1+ 版本的代码。现在tf更新了，就对源代码进行了一下改错，能够再tf 2.0 的环境下运行了

并添加了一次可以生成若干首诗的功能，和保存为文本的功能，新运行的诗不会覆盖以前的结果

只导入了单向的RNN、LSTM、和GRU。因为对tf框架不是很熟的缘故，也没有继续修改优化，后期打算用pytorch写一个

有一些训练好的模型保存起来了，main函数实用-w poem --no-train的参数就可以写诗了

主要是一个初期学习的作用，组会做着玩的。

就这么多